# Implementation Plans

Generated by the improve skill on 2026-07-25 against commit `07ab720`.
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
your row when done. The user pre-authorized implementing every planned
finding; plans were still selected on merit — see the rejected list below.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | CI, working lint, honest tests, bounded deps | P1 | M | — | DONE (reviewed; incl. approved pull-forward of 003's lastrowid fix) |
| 002 | Docs truth pass + CLAUDE.md | P2 | M | 001 (env only) | DONE (reviewed) |
| 003 | Library import corruption, schema versioning, ingest hardening | P1 | M | 001 | DONE (reviewed; 1 revision round for was_new flag) |
| 004 | Real quality scoring + memory ceilings removed | P1 | L | 001 | DONE (reviewed; np.stack criterion note — 2 unrelated matches remain by design) |
| 005 | CLI/solve/trails hardening + CLI & solve tests | P1 | L | 004 | DONE (reviewed) |
| 006 | GUI thread safety + shared numerics consolidation | P2 | L | 004, 005 | TODO |
| 007 | Export sidecars, outputs catalog, ratings, --flats | P2 | M | 003, 005 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (reason) | REJECTED (rationale)

## Dependency notes

- 001 first: it creates the verification gate (CI + honest tests + working
  ruff) every later plan is judged against, and does the mechanical lint
  sweep so later diffs stay reviewable.
- 004 before 005/006: all three touch `stack/integrate.py`, `cli.py`, and
  neighbors — sequential execution avoids merge conflicts, and 004's
  two-pass restructure is the foundation 006's stats consolidation edits.
- 003 before 007: 007 adds catalog write paths; 003 establishes the schema
  versioning those must respect.
- Execute sequentially on ONE branch (each plan builds on the previous
  plan's commits).

## Findings considered and rejected / deferred

Audit findings NOT planned, so nobody re-audits them:

- **PERF-04 (parallelize per-frame stages)** — deferred: highest wall-clock
  payoff but must land after 004's two-pass shape; do as its own plan next
  cycle. Sequence: `grade_session` and `scan_for_meteors` first.
- **PERF-07 (reuse detected stars across grade/register)** — deferred:
  MED-risk astroalign API change needing a registration-accuracy harness
  that doesn't exist yet; revisit after 004 beds in.
- **CORRECT-13 (per-file max normalization of float inputs)** — deferred:
  real smell, but the right canonical scale is a domain decision
  (fixed divisor vs header-derived vs session constant) the maintainer
  should make; `_normalize_to_ref` masks the worst of it today.
- **DEBT-07 (GUI re-implements pipeline orchestration)** — deferred:
  worthwhile consolidation, MED risk of GUI behavior change; do after 006's
  worker safety lands.
- **DEBT-09 (split cli.py)** — mostly rejected: the argparse wiring is
  flat, declarative, and fine; the worthwhile piece (Streak.to_dict,
  moving table renderers) can ride along a future change.
- **TEST-09 (coverage tooling)** — deferred: nice-to-have once CI exists;
  add `pytest-cov` + a CI upload in a follow-up, no threshold.
- **DEP-04 (dslr-extra CI job with a real raw fixture)** — deferred:
  needs a suitably-licensed tiny CR2 fixture; until then the ImportError
  path stays covered by design (001 keeps rawpy out of CI).
- **CORRECT-16 (trails dark ordering claim)** — corrected during vetting:
  the audit's demosaic-ordering claim was wrong (demosaic precedes the
  check); only the silent-skip logging gap was real → folded into 005.
- **gui/state.py redundant if/elif, writers.py unused `mode`, PNG
  docstring "16-bit" nit** — rejected: cosmetic, no failure mode.
- **Direction: external-tool adapter layer (DIR-02), multi-session
  integration (DIR-05), photometric color calibration (DIR-06), sky/
  foreground segmentation + trail-aware grading (DIR-07a)** — deferred as
  design/spike work requiring maintainer input (external binaries, network
  vs offline catalog, real cross-night data). DIR-07b (satellite flag
  during grading) is cheap once wanted — the detector already exists.
- **Batch/job-queue UI, Wi-Fi import, watch-folder** — rejected for now:
  shell loop covers batch for a single-user tool; device-network features
  need hardware spikes, not plans.
